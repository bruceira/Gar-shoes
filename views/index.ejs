<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
  <title>SHOES STORE</title>
</head>
<body>
  <div class="container">
    <h1>List of Shoes</h1>
    <a href="/shoes/new" class="btn btn-success">add new shoes</a>

       <% products.forEach(product=>{%> 
        <div class="card mt-4"> 
        <div class="card-body">
          <h4 class="card-title"><%= product.name.toUpperCase() %></h4>
          <div class="card-subtitle text-muted mb-2">
            <%= product.category %>
          </div>
          <div class="card-text mb-2"><%= product.price %> FBU</div>
          <a href="shoes/<%= product.slug %>" class="btn btn-primary">Read More</a>
          <a href="/shoes/edit/<%= product.id %>" class="btn btn-info">Edit</a>
          <form action="shoes/<%= product.id %>?_method=DELETE" method="POST" class="d-inline">
            <button type="submit" class="btn btn-danger" >Delete</button>
          </form>

          <div class="card-text mt-2"><%= product.addedAt.toLocaleDateString()%></div>
        </div>
        </div>
        <%})%>
  
  </div>


  
</body>
</html>